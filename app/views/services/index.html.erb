<h1>Service Tasks in <%= @category.name %></h1>
<ul>
  <% @service_tasks.each do |task| %>
    <li><%= link_to task.name, category_service_path(@category, task) %></li>
  <% end %>
</ul>
<%= link_to "Back to Categories", categories_path %>
